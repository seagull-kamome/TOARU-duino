/**
*/
#if !defined(ARDUINO__CONFIG_H)
#  define ARDUINO__CONFIG_H

// Hardware-Serialを自動的にincludeしたくない場合はここをコメントにする。
// その場合、ライブラリとしてHardwareSerialを明示的にincludeする必要がある。
#  include <HardwareSerial.h>

#endif